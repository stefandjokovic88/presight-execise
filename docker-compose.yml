services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      CLIENT_DIST: /app/client/dist
      NODE_ENV: production
    volumes:
      - sqlite_data:/app/server/data
    restart: unless-stopped

volumes:
  sqlite_data:
